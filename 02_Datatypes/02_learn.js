"use strict" // Treat all JS code as newer version

// alert(3 + 3); // It won't work as we are using Node.js, not browser

console.log(3 + 3);
console.log("Priyanshu"); // Code readability should be maintained


let name = "priyanshu";
let age = 20;
let isLoggedIn = false;
let state = null;

// Primitive Datatype
// Number => 2 ^ 53
// BigInt
// String => ""
// Boolean => true or false
// Null => Standalone value, Empty Value
// Undefined => Value not assigned
// Symbol => Uniqueness (To make something unique)

// Non-primitive Datatype
// Objects

console.log(typeof "alice");
console.log(typeof null); // Null ek object hai
console.log(typeof undefined); // Ye ek type hai but null type nai hai